<div id="clientForm">
    <form class="form-horizontal"  action="{% if clients.id is defined and clients.id%}{{ path('wizard_client_id', {'id': clients.id}) }}{% else %}{{ path('wizard_client') }}{% endif %}" method="post" {{ form_enctype(form) }}>
  <div class="control-group">

    {{ form_errors(form) }}
    {{ form_widget(form) }}
      <br>
    <div class="control-group">
       <button class="btn btn-success">Save</button>
    </div>
  </div>
</form>
</div>